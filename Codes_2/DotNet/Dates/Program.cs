using System;

namespace Dates
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            Ask the users for two dates. Then calculate the difference of these dates
            in both days and hours.
            */
            string date1, date2;
            DateTime dateTime1, dateTime2;
            float diffDays, diffOurs;

            System.Console.WriteLine("What is the date today?: ");
            date1 = Console.ReadLine();
            System.Console.WriteLine("What was the date yesterday?: ");
            date2 = Console.ReadLine();

            diffDays = (date)

            
            //System.Console.WriteLine(DateTime.Now); //To get the current date and time.
        }
    }
}
